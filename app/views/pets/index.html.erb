<h1>All Pets found... (<%= @pets.length %>)</h1>

<ul>
  <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet) %></li>
  <% end %>
</ul>

<%= link_to "Add a pet", new_pet_path, class: "btn btn-primary" %>
